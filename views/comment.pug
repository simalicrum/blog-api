mixin comment(commentarg)
  div.space-y-2.p-8
    p.text-sm.text-gray-500 #{commentarg.content}
    p.text-sm.text-gray-800 #{commentarg.author.first_name} #{commentarg.author.last_name} 
      span.text-gray-500 • #{commentarg.timestamp_formatted}